server:
  port: 8080
  servlet:
    register-default-servlet: true

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/bishe?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
  thymeleaf:
    cache: false

mybatis:
  type-aliases-package: linux.demo.pojo
  mapper-locations: classpath:/mapperxml/*.xml